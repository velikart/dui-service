# listDocumentationFiles

Возвращает список файлов документации из папки `dui/instruction`.

## Параметры

Нет.

## Возвращает

Массив строк с именами файлов (например, `dui-json-schema.json`).

## Когда использовать

- Чтобы узнать, какие документы доступны перед чтением конкретного файла.
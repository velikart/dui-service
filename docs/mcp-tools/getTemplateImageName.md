# getTemplateImageName

Возвращает имя файла изображения шаблона по UUID шаблона.

## Параметры

- `templateUUID` *(UUID, обязательно)* — идентификатор шаблона.

## Возвращает

Имя файла изображения (например, `template-1.png`).

## Когда использовать

- Для отображения/поиска превью шаблона.
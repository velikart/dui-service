# getTemplatePageJson

Возвращает JSON страницы шаблона по UUID шаблона.

## Параметры

- `templateUUID` *(UUID, обязательно)* — идентификатор шаблона.

## Возвращает

JSON-строку страницы шаблона.

## Когда использовать

- Для анализа или редактирования шаблонной страницы.
